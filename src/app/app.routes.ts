import { Routes } from '@angular/router';
import { ClientComponent } from './components/_client/client/client.component';

export const routes: Routes = [
  {
    path: '',
    component: ClientComponent,
  },
];
